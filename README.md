# Init

```bash
git submodule update --init --recursive
npm install hexo -g
npm install
```

# Create post

```bash
hexo new "My fist post."
```

# Local server for test

```bash
hexo server
```

# Deploy

```bash
hexo deploy -g
```
